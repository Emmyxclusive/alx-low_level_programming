#ifndef MAIN_H
# define MAIN_H

#include <stdio.h>

void positive_or_negetive(int i);
int largest_number(int a, int b, int c);
int print_remaining_days(int month, int day, int year);
int convert_month(int month, int day);
void print_times_table(int n);

#endif
